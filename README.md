# Rally Court

A responsive tennis club dashboard built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

To create a production build:

```bash
npm run build
```

## Install as a PWA

The production build includes a web app manifest and service worker. Open the deployed HTTPS site on a phone, then use the browser menu to choose **Add to Home Screen** or **Install app**.
